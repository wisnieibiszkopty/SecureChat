# MFA Web app

A simple, classic web application that implements Multi-Factor Authentication (MFA).

## Features
- User account management
- Secure password hashing using the scrypt algorithm
- QR code generation for integration with authenticator apps (e.g., Google Authenticator)
- Backup code generation, providing an alternative login method in case a user loses access to their authentication device